package ru.utoplov.vladimir.transport;

import com.bitwig.extension.controller.api.Transport;

public class Backward implements TransportCommand {
    @Override
    public void execute(Transport transport, boolean useShift) {
    }
}
